<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/fundo2</item>
        <item name="colorPrimaryDark">@color/fundo2</item>
        <item name="colorAccent">@color/fundo2</item>
    </style>

</resources>
